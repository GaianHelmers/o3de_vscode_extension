// ============================================================================
//  Shared constants.
// ============================================================================

/** This extension's fully-qualified id (publisher.name from package.json). Used as the
 *  cpptools CustomConfigurationProvider id and the C_Cpp.default.configurationProvider value. */
export const EXTENSION_ID = "GaianHelmers.o3de-development-tools";
